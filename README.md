# incubyte-tdd-assessment
This repo is for incubyte company's assessment

# String Calculator (Ruby) — TDD Kata
This is a small String Calculator implemented in Ruby with RSpec tests

- Clone the repo using `git clone <repo>`

- Navigate to the repo directory

- bundle install

Running Tests

- bundle exec rspec `path_to_test_file`